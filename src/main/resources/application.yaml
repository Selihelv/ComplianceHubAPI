
spring:
  application:
    name: ComplianceHubAPI

  datasource:
    url: jdbc:mysql://localhost:3306/compliance_hub_db
    username: root
    password: Ist34Bcn08+

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  web:
    error:
      include-stacktrace: never

